{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/MLSecOpsHub/agentic-attack-index/main/schema/incident.schema.json",
  "title": "Agentic Attack Index incident record",
  "description": "One real-world incident of an AI-agent-executed or AI-orchestrated cyberattack. Every factual field must be backed by a cited, resolvable source; unknown values stay null/empty.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "name",
    "summary",
    "date_disclosed",
    "status",
    "confidence",
    "category",
    "severity",
    "models",
    "model_families",
    "actor",
    "actor_type",
    "sources"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable kebab-case slug. Permanent: it is the citation key. Never renamed or recycled.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "minLength": 3,
      "maxLength": 80
    },
    "name": {
      "type": "string",
      "description": "Short human-readable incident name.",
      "minLength": 3,
      "maxLength": 160
    },
    "summary": {
      "type": "string",
      "description": "Defensive-framing summary at lifecycle level. No exploit code, payloads, prompts, or step-by-step offensive instructions.",
      "minLength": 20
    },
    "date_disclosed": {
      "type": "string",
      "description": "Date the incident was first publicly disclosed (ISO 8601).",
      "format": "date"
    },
    "status": {
      "type": "string",
      "description": "confirmed = multiple independent or first-party confirmation; reported = public reporting not independently confirmed; test-eval = lab test or evaluation, not a real-world attack.",
      "enum": ["confirmed", "reported", "test-eval"]
    },
    "confidence": {
      "type": "string",
      "description": "primary = first-party/primary sourcing; secondary = secondhand reporting; unverified = single unconfirmed source.",
      "enum": ["primary", "secondary", "unverified"]
    },
    "category": {
      "type": "string",
      "enum": [
        "ai-orchestrated-campaign",
        "autonomous-attack",
        "lab-escape-eval",
        "agent-hijack-prompt-injection",
        "infrastructure-abuse-supply-chain"
      ]
    },
    "severity": {
      "type": "string",
      "enum": ["critical", "high", "medium", "low"]
    },
    "models": {
      "type": "array",
      "description": "Specific model names as stated by sources (e.g. a versioned model identifier). Empty if sources do not name a model.",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "model_families": {
      "type": "array",
      "description": "Controlled vocabulary of model families involved. Use 'other' when sources name no covered family.",
      "items": {
        "type": "string",
        "enum": [
          "claude",
          "openai-gpt",
          "openai-codex",
          "gemini",
          "deepseek",
          "llama",
          "mistral",
          "hermes",
          "other"
        ]
      },
      "uniqueItems": true
    },
    "actor": {
      "type": "string",
      "description": "Threat actor as stated by sources. 'Unknown' is a valid value; never attribute beyond sources.",
      "minLength": 1
    },
    "actor_type": {
      "type": "string",
      "enum": [
        "nation-state",
        "cybercriminal",
        "single-operator",
        "lab-test-eval",
        "researcher",
        "unknown"
      ]
    },
    "autonomy_pct": {
      "description": "Percentage of the operation performed autonomously by the AI agent, only if a source states it. null when unstated.",
      "type": ["integer", "null"],
      "minimum": 0,
      "maximum": 100
    },
    "targets": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "orgs_affected": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "Number of organizations affected, only if a source states it."
        },
        "records_exfiltrated": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "Number of records exfiltrated, only if a source states it."
        },
        "sectors": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "countries": {
          "type": "array",
          "description": "ISO 3166-1 alpha-2 country codes of targets, as stated by sources.",
          "items": { "type": "string", "pattern": "^[A-Z]{2}$" },
          "uniqueItems": true
        }
      }
    },
    "geo": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "target": { "$ref": "#/$defs/geoPoint" },
        "origin": {
          "oneOf": [{ "$ref": "#/$defs/geoPoint" }, { "type": "null" }]
        }
      }
    },
    "lifecycle_phases": {
      "type": "array",
      "description": "Attack lifecycle phases observed, per sources. Descriptive only — no operational detail.",
      "items": {
        "type": "string",
        "enum": [
          "recon",
          "resource-dev",
          "initial-access",
          "execution",
          "credential-access",
          "privilege-escalation",
          "persistence",
          "exfiltration",
          "deception-social-eng",
          "impact"
        ]
      },
      "uniqueItems": true
    },
    "mappings": {
      "type": "object",
      "additionalProperties": false,
      "description": "External framework IDs exactly as published — never invent an ID. mitre_atlas preferred for AI-native techniques; mitre_attack for traditional TTPs.",
      "properties": {
        "mitre_atlas": {
          "type": "array",
          "items": { "type": "string", "pattern": "^AML\\.(T|TA|M|CS)[0-9]{4}(\\.[0-9]{3})?$" },
          "uniqueItems": true
        },
        "mitre_attack": {
          "type": "array",
          "items": { "type": "string", "pattern": "^(T|TA|S|G|M)[0-9]{4}(\\.[0-9]{3})?$" },
          "uniqueItems": true
        },
        "owasp_asi": {
          "type": "array",
          "items": { "type": "string", "minLength": 2 },
          "uniqueItems": true
        },
        "cve": {
          "type": "array",
          "items": { "type": "string", "pattern": "^CVE-[0-9]{4}-[0-9]{4,}$" },
          "uniqueItems": true
        },
        "aiid": {
          "type": "array",
          "description": "AI Incident Database incident numbers.",
          "items": { "type": "integer", "minimum": 1 },
          "uniqueItems": true
        }
      }
    },
    "impact": {
      "type": ["string", "null"],
      "description": "Stated impact (financial, data, operational), only as sources describe it."
    },
    "sources": {
      "type": "array",
      "description": "Every record needs at least one resolvable source. No source, no record.",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["title", "url", "publisher", "type"],
        "properties": {
          "title": { "type": "string", "minLength": 3 },
          "url": { "type": "string", "format": "uri", "pattern": "^https?://" },
          "archive_url": {
            "type": "string",
            "format": "uri",
            "pattern": "^https?://",
            "description": "Optional archival snapshot (e.g. Wayback Machine) so the record survives link rot. Must resolve independently of url."
          },
          "publisher": { "type": "string", "minLength": 1 },
          "type": {
            "type": "string",
            "enum": [
              "first-party-disclosure",
              "vendor-report",
              "government-advisory",
              "research-paper",
              "news",
              "blog",
              "other"
            ]
          },
          "date": { "type": "string", "format": "date" }
        }
      }
    },
    "added": {
      "type": "object",
      "additionalProperties": false,
      "required": ["date", "by"],
      "properties": {
        "date": { "type": "string", "format": "date" },
        "by": { "type": "string", "minLength": 1 }
      }
    },
    "last_updated": {
      "type": "string",
      "format": "date"
    }
  },
  "$defs": {
    "geoPoint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["lat", "lng", "label", "illustrative"],
      "properties": {
        "lat": { "type": "number", "minimum": -90, "maximum": 90 },
        "lng": { "type": "number", "minimum": -180, "maximum": 180 },
        "label": { "type": "string", "minLength": 1 },
        "illustrative": {
          "type": "boolean",
          "description": "true when the point is a sector/region centroid rather than a precise, source-stated location."
        }
      }
    }
  }
}
